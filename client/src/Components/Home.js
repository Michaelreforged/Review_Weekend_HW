import React from "react";

const Home = () => {

  return(
    <div>
      <h1>Home</h1>
      <p>Welcome to review homework where the model is based around computers. Base model is currently Owners, the computer's purpose, and what os it is running. Potential have many maybe added which would be components of the computers.
      </p>
    </div>
  )
}

export default Home;